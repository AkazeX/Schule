Das zap13-file "VtSys_4S" ist die Porgrammiervorlage für die Lernenden:

- Variablentabelle fertig vorhanden
- OB1 inkl. Netzwerke (leer) und Titel vorhanden
- alle anderen Bausteine müssen selber programmiert werden

WICHTIG: Wenn Sie diese Vorlage verwenden, dann passen Sie die IP-Adresse für die SPS an!